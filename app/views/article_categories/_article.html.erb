<h2><%= link_to article.title, article %></h2>
<p><%= truncate(article.body, length: 800, separator: ' ').html_safe %>
<%
if article.body.length > 800
%>
  &nbsp;<%= link_to "more", article %>
<%
end
%>
</p>
