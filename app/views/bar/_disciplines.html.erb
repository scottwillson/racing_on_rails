<%= tabs do |tabs|
  (overall_bar.children + [ overall_bar ]).sort_by(&:name).each do |bar|
    tabs.add(bar.discipline, bar_path(bar.year, Discipline[bar.discipline], race.try(:category_friendly_param)))
  end
end if overall_bar -%>
