<%= tabs(year) do |tabs|
  years = Event.find_all_years << RacingAssociation.current.effective_year << year

  years.uniq.sort.reverse.each do |y|
    tabs.add(
      y,
      bar_path(year: y, category: race.try(:category_friendly_param), discipline: Discipline[race.try(:discipline)])
    )
  end
end -%>
