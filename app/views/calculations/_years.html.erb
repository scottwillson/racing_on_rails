<%- if calculation&.years&.many? -%>
  <%= tabs(calculation.year) do |tabs|
    calculation.years.sort.reverse.each do |year|
      tabs.add(
        year.to_s.titleize,
        friendly_calculation_path(calculation.key, year)
      )
    end
  end -%>
<%- end -%>
