<%= tabs(discipline) do |tabs|
  (overall_bar.children + [ overall_bar ]).sort_by(&:name).each do |b|
    tabs.add(
      b.discipline,
      bar_path(
        year: b.year,
        discipline: Discipline[b.discipline],
        category: race.try(:category_friendly_param)
      )
    )
  end
end if overall_bar -%>
