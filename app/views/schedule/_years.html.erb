<%= tabs do |tabs|
  Event.find_all_years.each do |year|
    if year == @year
      tabs.add(year.to_s, :year => nil, :discipline => @discipline)
    else
      tabs.add(year.to_s, :year => year, :discipline => @discipline)
    end
  end
end -%>
