<% if sort_by.present? %>
<th class="<%= style_class %>"><%= link_to(title, {:sort_by => "#{sort_by.join(',')}", :sort_direction => sort_direction}.merge(sort_params)) -%></th>
<% else -%>
<th class="<%= style_class %>"><%= title -%></th>
<% end -%>
