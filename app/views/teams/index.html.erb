<% @page_title = "Teams" %>

<h2>OBRA-registered teams in Oregon and SW Washington.</h2>

<p align="left">A list of <%= link_to "clubs rides", "http://#{STATIC_HOST}/teams/rides.html", :class => "obvious" %> is available if you're looking for a group ride.</p>

<% table :caption => "Teams", :id => "teams" do %>
  <tr>
    <th>Name</th>
    <th>Sponsors</th>
    <th>Website</th>
    <th>Contact</th>
    <th></th>
  </tr>
  <%= render :partial => "team", :collection => @teams.sort { |x, y| x.name.downcase <=> y.name.downcase || x <=> y } %>
<% end -%>

